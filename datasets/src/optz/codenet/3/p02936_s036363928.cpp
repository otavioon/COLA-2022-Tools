#include <bits/stdc++.h>
using namespace std;


void solve(long long N, long long Q, std::vector<long long> a, std::vector<long long> b, std::vector<long long> p, std::vector<long long> x){

}

// Generated by 1.1.6 https://github.com/kyuridenamida/atcoder-tools  (tips: You use the default template now. You can remove this line by using your custom template)
int main(){
    long long N,Q;cin >> N >> Q;
    vector<pair<long long,long long>> edge;
    vector<long long> point(N,0);
    for(long long i = 0;i<N-1;i++){
        long long in,out;cin >> in >> out;
        edge.push_back(make_pair(in-1,out-1));
    }
    
    sort(edge.begin(),edge.end());
    for(long long i = 0;i < Q;i++){
        long long no,p;cin >> no >> p;
        point[no-1] += p;
    }

    for(long long i = 0;i < edge.size();i++){
        point[edge[i].second] += point[edge[i].first];
    }

    cout << point[0];
    for(long long i = 1;i < N;i++) cout << " " << point[i];
    cout << endl;
}
